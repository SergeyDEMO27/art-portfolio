import React from "react";
import "../../../styles/UI/buttons/closeButton.scss";

const CloseButton: React.FC = () => {
  return <button className='close-button' type='button'></button>;
};

export default CloseButton;
